{{outlet}}

{{#if flashMessages.queue}}
  <div local-class="flash-messages">
    {{#each flashMessages.queue as |flash|}}
      {{#flash-message flash=flash local-class="flash-messages-item"}}
        {{ui-flash-message flash=flash}}
      {{/flash-message}}
    {{/each}}
  </div>
{{/if}}